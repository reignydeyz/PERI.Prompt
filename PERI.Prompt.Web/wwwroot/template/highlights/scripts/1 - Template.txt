update Template 	set DateInactive = date('now');

insert into Template(Name, DateInactive) values('TunogPilipino', null);

--------------------
insert into Section(TemplateId, Name)
select TemplateId, 'About' from Template where DateInactive is null limit 1;

insert into SectionItem(SectionId, Title, Body, [Order], CreatedBy, DateCreated, ModifiedBy, DateModified)
select Section.SectionId, 'Default', 'Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum.', 0, 'System', date('now'), 'System', date('now')
from Section 
inner join Template on Template.TemplateId = Section.TemplateId
where Template.Name = 'TunogPilipino'
and Section.Name = 'About';

--------------------
insert into Section(TemplateId, Name)
select TemplateId, 'News Feed' from Template where DateInactive is null limit 1;

insert into SectionItem(SectionId, Title, Body, [Order], CreatedBy, DateCreated, ModifiedBy, DateModified)
select Section.SectionId, 'Default', '--PASTE YOUR HTML WIDGET--', 0, 'System', date('now'), 'System', date('now')
from Section 
inner join Template on Template.TemplateId = Section.TemplateId
where Template.Name = 'TunogPilipino'
and Section.Name = 'News Feed';

--------------------
insert into Section(TemplateId, Name)
select TemplateId, 'Partners' from Template where DateInactive is null limit 1;

insert into SectionProperty(SectionId, Name)
select Section.SectionId, 'fa-icon' 
from Section 
inner join Template on Template.TemplateId = Section.TemplateId
where Template.Name = 'TunogPilipino'
and Section.Name = 'Partners';

insert into SectionProperty(SectionId, Name)
select Section.SectionId, 'link' 
from Section 
inner join Template on Template.TemplateId = Section.TemplateId
where Template.Name = 'TunogPilipino'
and Section.Name = 'Partners';

--------------------
insert into Section(TemplateId, Name)
select TemplateId, 'Social Icons' from Template where DateInactive is null limit 1;

insert into SectionProperty(SectionId, Name)
select Section.SectionId, 'fa-icon' 
from Section 
inner join Template on Template.TemplateId = Section.TemplateId
where Template.Name = 'TunogPilipino'
and Section.Name = 'Social Icons';

insert into SectionProperty(SectionId, Name)
select Section.SectionId, 'icon link' 
from Section 
inner join Template on Template.TemplateId = Section.TemplateId
where Template.Name = 'TunogPilipino'
and Section.Name = 'Social Icons';